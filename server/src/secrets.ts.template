const secrets = {
    spotify: {
        secret: "REPLACE WITH THE ACTUAL SECRET"
    },
    db: {
        password: "REPLACE_DB_PASSWORD"
    },
    certPath: "REPLACE_CERT_PATH"
};

export default secrets;